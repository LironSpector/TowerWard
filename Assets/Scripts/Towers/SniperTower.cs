using UnityEngine;

/// <summary>
/// Description:
/// Represents a Sniper Tower—a specialized type of ProjectileTower with high precision.
/// Currently, this class relies entirely on the base class functionality without custom logic.
/// </summary>
public class SniperTower : ProjectileTower
{
    /// <summary>
    /// Called when the script instance is being loaded.
    /// Invokes the base Awake implementation.
    /// </summary>
    protected override void Awake()
    {
        base.Awake();
    }

    /// <summary>
    /// Called before the first frame update.
    /// Invokes the base Start implementation.
    /// </summary>
    protected override void Start()
    {
        base.Start();
    }
}
